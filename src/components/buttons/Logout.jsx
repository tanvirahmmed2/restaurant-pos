'use client'
import React from 'react'

const Logout = () => {
  return (
    <button className='w-auto flex items-center justify-between gap-3 cursor-pointer'>Logout</button>
  )
}

export default Logout
